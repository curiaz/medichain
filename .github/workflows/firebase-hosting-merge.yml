# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
#
# NOTE: This workflow is DISABLED - using cd-production.yml instead.
# The primary CD pipeline is now in cd-production.yml which runs after CI passes.

name: Deploy to Firebase Hosting on merge
on:
  # DISABLED - Using cd-production.yml instead
  # push:
  #   branches:
  #     - master
  workflow_dispatch:  # Keep for manual trigger only if needed
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MEDICHAIN_8773B }}
          channelId: live
          projectId: medichain-8773b
