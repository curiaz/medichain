name: Test CD - Simple

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test CD Workflow
        run: |
          echo "✅ CD workflow is working!"
          echo "This is a test to verify GitHub Actions can see the workflow"
          echo "If you see this, the workflow system is working!"

